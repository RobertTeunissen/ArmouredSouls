/**
 * Barrel export for shared backend type definitions.
 *
 * These types describe JSON structures stored in Prisma `Json` fields.
 * Import from here (e.g. `import { StableMetric } from '../../types'`)
 * instead of defining local copies.
 */

export * from './snapshotTypes';
export * from './battleLogTypes';
