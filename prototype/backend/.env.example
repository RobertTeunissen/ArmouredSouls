# Database
# Connection pool settings are configured in the URL:
# - connection_limit: Max connections (default: num_cpus * 2 + 1)
# - pool_timeout: Seconds to wait for connection (default: 10)
# Example with pool settings:
# DATABASE_URL="postgresql://armouredsouls:password@localhost:5432/armouredsouls?connection_limit=10&pool_timeout=20"
DATABASE_URL="postgresql://armouredsouls:password@localhost:5432/armouredsouls"

# JWT Secret
JWT_SECRET="your-secret-key-change-this-in-production"

# Server
PORT=3001
NODE_ENV=development
