{
  "_description": "No current pnpm production advisories are allowlisted. Add an entry only after a current audit identifies a vulnerability that is reviewed, unreachable or otherwise accepted, with a bounded re-review date.",
  "allowlist": [],
  "_entrySchema": {
    "id": "GHSA advisory ID (e.g., GHSA-xxxx-xxxx-xxxx)",
    "package": "Affected npm package name",
    "justification": "Why this vulnerability is accepted",
    "reviewDate": "ISO date when the vulnerability was last reviewed (YYYY-MM-DD)",
    "nextReviewDate": "ISO date when the vulnerability should be re-evaluated (YYYY-MM-DD)"
  }
}
